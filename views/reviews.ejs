<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/css/home.css">
</head>
<body>
    <div class="uk-container"> 
        <div class="uk-section uk-section-large">
            <% if (resultado.length == 0) { %>
                <h1>You Havent made any reviews yet...</h1>
            <%} else { %>
                <h1 class="uk-text-center"> <%= resultado[0].usuario.nombre_de_usuario %> 's reviews</h1>

                <% for(let i=0; i < resultado.length; i++) { %>
                    <div class="uk-card uk-card-secondary uk-margin">
                        <div class="uk-carde-badge uk-label uk-margin-bottom" style="color: white;"> Score: <%= resultado[i].puntaje_sobre_pelicula %></div>
                        <br>
                        <div class="uk-card-body">
                            <a href="/detallePelicula?id=<%= resultado[i].id_pelicula %>" class="uk-margin-top"> Id Pelicula: <%= resultado[i].id_pelicula %> </a>
                            <h4 class="uk-margin-top"> <%= resultado[i].texto_de_reseña %> </h4>
                        </div>
                        <div class="uk-card-footer uk-flex uk-flex-between">
                            <a class="uk-text-success" href="/usuario/reviews/edit/<%= resultado[i].id%>">Edit</a>
                            <a class="uk-text-primary" href="/detallePelicula?id=<%= resultado[i].id_pelicula %>">Go to Movie</a>
                            <a class="uk-text-danger" href="/usuario/reviews/delete/<%= resultado[i].id %>">Delete</a>
                        </div>
                    </div>

                <%}%>

            <%}%>
        </div>
    </div>
   
   
            
            
</body>

</html>